extends ../layout.pug 

block content
  .del-msg
    p 
      | Are you sure you want to delete 
      span.title= book.title
      | ?
    form(action="/books/" + book.id + "/delete", method="post")
      a(href="/books/" + book.id) No! Take me back! 
      | 
      input.btn.btn-primary(type="submit", value = "Yes, I'm sure")